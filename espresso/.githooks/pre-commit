#!/usr/bin/env bash
if git diff --cached --name-only | grep -q "^espresso/.env$"; then
  echo "⚠️ WARNING: You updated .env."
  echo "You MUST also update image versions in tee-op-deploy, or create a ticket there."
  echo "See .env header for details."
  echo "After either updating tee-op-deploy or create a ticket, commit with: git commit --no-verify"
fi
